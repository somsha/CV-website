<table class="work-table">
    <thead>
        <tr>
            <th>Company</th>
            <th>Position</th>
            <th>Description</th>
            <th>Start Date</th>
            <th>End Date</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {{#each work}}
        <tr class="work-row">
            <td>{{this.company}}</td>
            <td>{{this.position}}</td>
            <td>{{this.description}}</td>
            <td>{{this.startDate}}</td>
            <td>{{this.endDate}}</td>
            <td>
                <form action="/cv/work/remove" method="post">
                    <input type="hidden" name="id" value="{{this.id}}">
                    <button type="submit">Remove</button>
                </form>
            </td>
        </tr>
        {{/each}}
        <!-- Empty row for adding new work -->
        <tr class="work-row">
            <td><input type="text" class="edit-input" placeholder="Company" name="workCompany" required></td>
            <td><input type="text" class="edit-input" placeholder="Position" name="workPosition" required></td>
            <td><input type="text" class="edit-input" placeholder="Description" name="workDescription" required></td>
            <td><input type="date" class="edit-input" placeholder="Start Date" name="workStartDate" required></td>
            <td><input type="date" class="edit-input" placeholder="End Date" name="workEndDate" required></td>
            <td><button type="submit" id="addworkButton">Add</button></td>
        </tr>
    </tbody>
</table>